

# 默认环境收集器

-----

这将添加一个 `default` 环境，类型设为 [virtual](../environment/virtual.md)，并始终应用。

## 配置

环境收集器插件的名称是 `default`。

```toml config-example
[tool.hatch.env.collectors.default]
```

## 选项

目前没有可用的选项。